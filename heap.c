#include <stdio.h>


int isHeap(int array[])
{
    int size = sizeof(array_name) / sizeof(array_name[index]);
    
    printf("%d", size);
}



int main()
{
    int arr[] = {1, 2, 3, 4, 5, 10};
    isHeap(arr);

    return 0;
}